#include "main.h"
/**
 * -isdigit - checks through for (0 - 9)
 * @c: prints the digits
 *
 *  return 0 or 1
 */
int _isdigit(int c)
{
	if ((c <= 48) && (c >= 57))
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
